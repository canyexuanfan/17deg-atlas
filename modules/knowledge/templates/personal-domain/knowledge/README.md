# 知识

这里是日常阅读和整理知识的工作区。

- `inbox/`：刚收到、尚未判断去向的内容。
- `raw/`：按来源类型自动整理的原始材料，保留原件。
- `library/`：经过确认、适合长期使用的知识。
- `wiki/`：由原始材料编译出的来源摘要、原子卡片和主题页，默认为候选内容。

每份文档都带有访问范围和生命周期属性，用于说明谁能看到、保存多久；这两项是文档属性，不是工作区里的文件夹。

Agent 会先保留原件；候选内容需要你确认后才会成为正式知识。遇到需要判断的内容，Agent 会先询问。
