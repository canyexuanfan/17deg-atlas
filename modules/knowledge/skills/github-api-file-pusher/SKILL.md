---
name: github-api-file-pusher
description: Internal compatibility capability for deterministic knowledge operations. Use only when called by $17deg-atlas-local or $17deg-atlas-remote, or when maintaining legacy integrations.
---

# GitHub API File Pusher

这是内部兼容能力，只能由 17deg-atlas-local 或 17deg-atlas-remote 调用。普通用户不要直接安装。

- 不得创建第三套安装、认证或环境判断流程
- 调用方已明确入口角色，冲突必须停止
- 远端真实改动仍需主入口确认
- 返回脱敏结果
