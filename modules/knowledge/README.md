# 17deg Atlas Knowledge

17deg Atlas 的知识库能力模块，为本地和远端 Agent 提供统一的创建、连接、保存与检索入口。

普通使用者只需向 Agent 表达目标。首次配置会自动检查所需工具和 GitHub 登录状态，并连接或准备私有知识空间，无需手工配置技术参数。

配置完成后，直接用自然语言保存、搜索或整理内容。内容的公开、分享与授权边界由使用者决定。

当你认可某个知识条目有效，可以让 Agent 把它变成可复用能力。能力安装位置由你选择：仅在当前项目、全局可用，或让 Agent 根据运行时场景推荐。首次全局安装会征求你的确认。
